<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 맵핑될 DAO 인터페이스의 Full name 을 줍니다. -->
<mapper namespace="com.common.www">
	<!-- 메소드 이름 가지고 와서 작성 -->
	<select id="getStore" resultType="com.common.www.dto.commonDTO">
		SELECT STORECODE , INLOCATION ,
		TEL , DAILYSALES ,MONTHLYSALES
		FROM COOSTORE
	</select>

	<select id="getItem" resultType="com.common.www.dto.commonDTO2">
		SELECT GOODSNUMBER,
		GOODSNAME,
		PRICE,
		NOWSTOCK,
		INPUTGOODS,
		EXPIRATIONDATE,
		SOLDNUM,
		NEWITEM
		FROM COOGOODS
	</select>
</mapper>